#include <iostream>

using namespace std;

int main() {
    int a,b,suma;
    cout<<"Inserte el valor de a: ";
    cin >> a;
    cout<< "Inserte el valor de b: ";
    cin>>b;
    suma = a + b; //Sumar ambos numeros
    cout<<"Valor de a: "<<a<<endl<<"Valor de b: "<<b<<endl;
    cout<<"El resultado es: "<<suma<<endl; //Resultado de la suma
    return 0;
}